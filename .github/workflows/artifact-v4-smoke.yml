# .github/workflows/artifact-v4-smoke.yml
name: artifact-v4-smoke
on: workflow_dispatch
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok > file.txt
      - uses: actions/upload-artifact@v4
        with:
          name: smoke
          path: file.txt
